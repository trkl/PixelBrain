{
  "name": "forritaverkfrodi-react-starter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "acorn": "^6.0.7",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "docker": "yarn build && docker build . -t react-docker && docker run -p 8000:80 react-docker"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
